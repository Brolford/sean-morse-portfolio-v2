---
const { project, cardClass } = Astro.props;
const isComingSoon = project.comingSoon;
const isLocked = project.passwordProtected;
const hasImage = !!project.image;
const isClickable = !isComingSoon && !isLocked && project.problem;
const href = isClickable ? `/project/${project.slug}` : undefined;
const Tag = isClickable ? 'a' : 'div';
---
<!-- Single project card with gradient, overlay, and hover states -->
<a
  href={href}
  class={`project-card group relative overflow-hidden rounded block ${cardClass} ${isClickable ? 'cursor-pointer' : 'cursor-default'}`}
  style="text-decoration: none; color: inherit;"
  data-slug={project.slug}
  data-coming-soon={isComingSoon || false}
>
  <!-- Background: image if available, gradient fallback -->
  {hasImage ? (
    <img
      src={project.image}
      alt={project.title}
      class="project-card-bg w-full h-full transition-transform duration-400"
      style={`object-fit: cover; object-position: ${project.imagePosition || 'center'}; transition-timing-function: var(--ease-out);`}
      loading="lazy"
      transition:name={`project-bg-${project.slug}`}
    />
  ) : (
    <div
      class={`project-card-bg w-full h-full transition-transform duration-400 ${project.gradientClass}`}
      style="transition-timing-function: var(--ease-out);"
      transition:name={`project-bg-${project.slug}`}
    ></div>
  )}

  <!-- Overlay (always visible) -->
  <div class="absolute inset-0" style={`background: linear-gradient(to top, rgba(0,0,0,0.5) 0%, transparent 55%);${isComingSoon ? 'opacity: 0.5;' : ''}`}>
    <!-- Lock indicator for password-protected projects -->
    {isLocked && (
      <div class="absolute top-4 right-4 flex items-center gap-1.5" style="opacity: 0.5;">
        <svg width="12" height="14" viewBox="0 0 12 14" fill="none" xmlns="http://www.w3.org/2000/svg">
          <rect x="0.5" y="5.5" width="11" height="8" rx="1.5" stroke="white" stroke-width="1"/>
          <path d="M3 5.5V4C3 2.34 4.34 1 6 1C7.66 1 9 2.34 9 4V5.5" stroke="white" stroke-width="1" stroke-linecap="round"/>
          <circle cx="6" cy="9.5" r="1" fill="white"/>
        </svg>
      </div>
    )}

    <!-- Bottom-left info stack -->
    <div class="absolute bottom-5 left-5 right-5">
      <!-- Descriptor (shows on hover, positioned above the title) -->
      {!isComingSoon && project.descriptor && (
        <div class="project-descriptor opacity-0 translate-y-2 transition-all duration-300 mb-2" style="font-family: var(--font-display); font-weight: 300; font-style: italic; font-size: 15px; color: rgba(255,255,255,0.8);">
          {project.descriptor}
        </div>
      )}
      <div style="font-family: var(--font-body); font-size: 10px; text-transform: uppercase; letter-spacing: 0.12em; color: rgba(255,255,255,0.75);">
        {project.category}
      </div>
      <div style="font-family: var(--font-display); font-weight: 300; font-style: italic; font-size: clamp(22px,2.5vw,40px); color: white; line-height: 1.15;">
        {project.title}
      </div>
      <div style="font-family: var(--font-body); font-size: 10px; text-transform: uppercase; letter-spacing: 0.12em; color: rgba(255,255,255,0.75); margin-top: 2px;">
        {project.year}
      </div>
    </div>
  </div>
</a>

<style>
  .project-card:not([data-coming-soon="true"]):hover {
    transform: scale(1.02);
    transition: transform 400ms var(--ease-out);
  }
  .project-card:not([data-coming-soon="true"]):hover .project-card-bg {
    transform: scale(1.04);
  }
  .project-card:not([data-coming-soon="true"]):hover .project-descriptor {
    opacity: 1;
    transform: translateY(0);
  }
</style>
